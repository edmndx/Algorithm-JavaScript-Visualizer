[
  {
    "id": "insertion-sort",
    "name": "Insertion sort",
    "category": "Sorting",
    "description": "Build a sorted prefix one item at a time.",
    "code": "const values = [8, 3, 5, 1];\n\nfor (let index = 1; index < values.length; index++) {\n  const current = values[index];\n  let position = index - 1;\n\n  while (position >= 0 && values[position] > current) {\n    values[position + 1] = values[position];\n    position--;\n  }\n\n  values[position + 1] = current;\n}\n\nconsole.log(values);"
  },
  {
    "id": "binary-search",
    "name": "Binary search",
    "category": "Searching",
    "description": "Narrow a sorted range until the target is found.",
    "code": "const values = [1, 3, 5, 8, 13];\nconst target = 8;\nlet left = 0;\nlet right = values.length - 1;\nlet result = -1;\n\nwhile (left <= right) {\n  const middle = Math.floor((left + right) / 2);\n\n  if (values[middle] === target) {\n    result = middle;\n    break;\n  }\n\n  if (values[middle] < target) {\n    left = middle + 1;\n  } else {\n    right = middle - 1;\n  }\n}\n\nconsole.log(result);"
  },
  {
    "id": "in-order-traversal",
    "name": "In-order traversal",
    "category": "Trees",
    "description": "Visit the left subtree, root, and right subtree in order.",
    "code": "const tree = {\n  value: 4,\n  left: { value: 2, left: null, right: null },\n  right: { value: 6, left: null, right: null }\n};\n\nfunction traverse(node) {\n  if (!node) return;\n  traverse(node.left);\n  console.log(node.value);\n  traverse(node.right);\n}\n\ntraverse(tree);"
  },
  {
    "id": "breadth-first-search",
    "name": "Breadth-first search",
    "category": "Graphs",
    "description": "Visit graph nodes level by level.",
    "code": "const graph = {\n  A: ['B', 'C'],\n  B: ['D'],\n  C: ['E'],\n  D: [],\n  E: []\n};\n\nconst queue = ['A'];\nconst visited = new Set(queue);\n\nwhile (queue.length > 0) {\n  const node = queue.shift();\n  console.log(node);\n\n  for (const neighbor of graph[node]) {\n    if (!visited.has(neighbor)) {\n      visited.add(neighbor);\n      queue.push(neighbor);\n    }\n  }\n}"
  },
  {
    "id": "generate-permutations",
    "name": "Generate permutations",
    "category": "Backtracking",
    "description": "Build every ordering by trying and undoing each choice.",
    "code": "function permutations(values, path = []) {\n  if (values.length === 0) {\n    console.log(path);\n    return;\n  }\n\n  for (let index = 0; index < values.length; index++) {\n    const remaining = values.filter((_, itemIndex) => itemIndex !== index);\n    permutations(remaining, [...path, values[index]]);\n  }\n}\n\npermutations([1, 2, 3]);"
  }
]
