export interface AlgorithmCatalogEntry {
  id: string;
  name: string;
  category: string;
  description: string;
  code: string;
}

export interface CommandChip {
  label: string;
  command: string;
}
